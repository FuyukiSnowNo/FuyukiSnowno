name: Generate Stats

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No workflow needed"

#      - uses: actions/checkout@v3
#    - uses: anuraghazra/github-readme-stats@v3
#       with:
#          token: ${{ secrets.GH_TOKEN }}
